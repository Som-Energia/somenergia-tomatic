const m = require('./mithril/graella')
/*
class app {
  constructor() {
    this.els = ['first itemisim']
  }

  view() {
    return [
      m(
        'div',
        m(
          'button',
          {
            onclick: () => {
              this.els.push('another item')
            },
          },
          'add item'
        ),

        m(
          'ul',
          this.els.map((item) => m('li', item))
        )
      ),
    ]
  }
}

m.mount(document.body, app)
*/
