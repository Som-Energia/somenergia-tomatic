+lidia dl # absencia odoo (vacances, baixa...)
+lidia dm # absencia odoo (vacances, baixa...)
+lidia dx # absencia odoo (vacances, baixa...)
+lidia dj # absencia odoo (vacances, baixa...)
+lidia dv # absencia odoo (vacances, baixa...)
+carol dl # absencia odoo (vacances, baixa...)
+carol dm # absencia odoo (vacances, baixa...)
+carol dx # absencia odoo (vacances, baixa...)
+carol dj # absencia odoo (vacances, baixa...)
+carol dv # absencia odoo (vacances, baixa...)
+erola dl # absencia odoo (vacances, baixa...)
+erola dm # absencia odoo (vacances, baixa...)
+erola dx # absencia odoo (vacances, baixa...)
+erola dj # absencia odoo (vacances, baixa...)
+erola dv # absencia odoo (vacances, baixa...)
+gemma dl # absencia odoo (vacances, baixa...)
+gemma dm # absencia odoo (vacances, baixa...)
+gemma dx # absencia odoo (vacances, baixa...)
+gemma dj # absencia odoo (vacances, baixa...)
+gemma dv # absencia odoo (vacances, baixa...)
+gabriel dx # absencia odoo (vacances, baixa...)
+gabriel dj # absencia odoo (vacances, baixa...)
+gabriel dv # absencia odoo (vacances, baixa...)
+gabriel dl # absencia odoo (vacances, baixa...)
+gabriel dm # absencia odoo (vacances, baixa...)
+annaserra dl # absencia odoo (vacances, baixa...)
+annaserra dm # absencia odoo (vacances, baixa...)
+annaserra dx # absencia odoo (vacances, baixa...)
+annaserra dj # absencia odoo (vacances, baixa...)
+annaserra dv # absencia odoo (vacances, baixa...)
+nuriapuigvert dj # absencia odoo (vacances, baixa...)
+nuriapuigvert dv # absencia odoo (vacances, baixa...)
+claran dl # absencia odoo (vacances, baixa...)
+claran dm # absencia odoo (vacances, baixa...)
+claran dx # absencia odoo (vacances, baixa...)
+carles dl # absencia odoo (vacances, baixa...)
+carles dm # absencia odoo (vacances, baixa...)
+carles dx # absencia odoo (vacances, baixa...)
+carles dj # absencia odoo (vacances, baixa...)
+carles dv # absencia odoo (vacances, baixa...)
+juditj dl # absencia odoo (vacances, baixa...)
+juditj dm # absencia odoo (vacances, baixa...)
+juditj dx # absencia odoo (vacances, baixa...)
+juditj dj # absencia odoo (vacances, baixa...)
+juditj dv # absencia odoo (vacances, baixa...)
+joansarola dl # absencia odoo (vacances, baixa...)
+joansarola dm # absencia odoo (vacances, baixa...)
+joansarola dx # absencia odoo (vacances, baixa...)
+joansarola dj # absencia odoo (vacances, baixa...)
+joansarola dv # absencia odoo (vacances, baixa...)
+carme dl # absencia odoo (vacances, baixa...)
+carme dm # absencia odoo (vacances, baixa...)
+carme dx # absencia odoo (vacances, baixa...)
+carme dj # absencia odoo (vacances, baixa...)
+carme dv # absencia odoo (vacances, baixa...)
+annaoller dl # absencia odoo (vacances, baixa...)
+annaoller dm # absencia odoo (vacances, baixa...)
+annaoller dx # absencia odoo (vacances, baixa...)
+annaoller dj # absencia odoo (vacances, baixa...)
+annaoller dv # absencia odoo (vacances, baixa...)
+marta dl # absencia odoo (vacances, baixa...)
+marta dm # absencia odoo (vacances, baixa...)
+marta dx # absencia odoo (vacances, baixa...)
+marta dj # absencia odoo (vacances, baixa...)
+marta dv # absencia odoo (vacances, baixa...)
+vero dm # absencia odoo (vacances, baixa...)
+vero dx # absencia odoo (vacances, baixa...)
+vero dj # absencia odoo (vacances, baixa...)
+vero dv # absencia odoo (vacances, baixa...)
+monicag dl # absencia odoo (vacances, baixa...)
+monicag dm # absencia odoo (vacances, baixa...)
+monicag dx # absencia odoo (vacances, baixa...)
+monicag dj # absencia odoo (vacances, baixa...)
+monicag dv # absencia odoo (vacances, baixa...)
+judit dx # absencia odoo (vacances, baixa...)
+judit dj # absencia odoo (vacances, baixa...)
+judit dv # absencia odoo (vacances, baixa...)
+israel dl # absencia odoo (vacances, baixa...)
+israel dm # absencia odoo (vacances, baixa...)
+israel dx # absencia odoo (vacances, baixa...)
+israel dj # absencia odoo (vacances, baixa...)
+israel dv # absencia odoo (vacances, baixa...)
+silvia dl # absencia odoo (vacances, baixa...)
+silvia dm # absencia odoo (vacances, baixa...)
+silvia dx # absencia odoo (vacances, baixa...)
+silvia dj # absencia odoo (vacances, baixa...)
+silvia dv # absencia odoo (vacances, baixa...)
+david dl # absencia odoo (vacances, baixa...)
+david dm # absencia odoo (vacances, baixa...)
+david dx # absencia odoo (vacances, baixa...)
+david dj # absencia odoo (vacances, baixa...)
+david dv # absencia odoo (vacances, baixa...)
+marcm dv # absencia odoo (vacances, baixa...)
+eduard dl # absencia odoo (vacances, baixa...)
+eduard dm # absencia odoo (vacances, baixa...)
+eduard dx # absencia odoo (vacances, baixa...)
+eduard dj # absencia odoo (vacances, baixa...)
+eduard dv # absencia odoo (vacances, baixa...)
+otilia dl # absencia odoo (vacances, baixa...)
+otilia dm # absencia odoo (vacances, baixa...)
+otilia dx # absencia odoo (vacances, baixa...)
+otilia dj # absencia odoo (vacances, baixa...)
+otilia dv # absencia odoo (vacances, baixa...)
+alex dl # absencia odoo (vacances, baixa...)
+alex dm # absencia odoo (vacances, baixa...)
+alex dx # absencia odoo (vacances, baixa...)
+alex dj # absencia odoo (vacances, baixa...)
+alex dv # absencia odoo (vacances, baixa...)
+naiara dl # absencia odoo (vacances, baixa...)
+naiara dm # absencia odoo (vacances, baixa...)
+naiara dx # absencia odoo (vacances, baixa...)
+naiara dj # absencia odoo (vacances, baixa...)
+naiara dv # absencia odoo (vacances, baixa...)
+joana dl # absencia odoo (vacances, baixa...)
+joana dm # absencia odoo (vacances, baixa...)
+joana dx # absencia odoo (vacances, baixa...)
+joana dj # absencia odoo (vacances, baixa...)
+joana dv # absencia odoo (vacances, baixa...)
+jairo dl # absencia odoo (vacances, baixa...)
+jairo dm # absencia odoo (vacances, baixa...)
+jairo dx # absencia odoo (vacances, baixa...)
+jairo dj # absencia odoo (vacances, baixa...)
+jairo dv # absencia odoo (vacances, baixa...)
+annap dl # absencia odoo (vacances, baixa...)
+annap dm # absencia odoo (vacances, baixa...)
+annap dx # absencia odoo (vacances, baixa...)
+annap dj # absencia odoo (vacances, baixa...)
+annap dv # absencia odoo (vacances, baixa...)
+melania dl # absencia odoo (vacances, baixa...)
+melania dm # absencia odoo (vacances, baixa...)
+melania dx # absencia odoo (vacances, baixa...)
+melania dj # absencia odoo (vacances, baixa...)
+melania dv # absencia odoo (vacances, baixa...)
+joanagomis dl # absencia odoo (vacances, baixa...)
+joanagomis dm # absencia odoo (vacances, baixa...)
+joanagomis dx # absencia odoo (vacances, baixa...)
+joanagomis dj # absencia odoo (vacances, baixa...)
+joanagomis dv # absencia odoo (vacances, baixa...)
+sergiv dl # absencia odoo (vacances, baixa...)
+sergiv dm # absencia odoo (vacances, baixa...)
+sergiv dx # absencia odoo (vacances, baixa...)
+sergiv dj # absencia odoo (vacances, baixa...)
+sergiv dv # absencia odoo (vacances, baixa...)
+annarodon dx # absencia odoo (vacances, baixa...)
+annarodon dj # absencia odoo (vacances, baixa...)
+annarodon dv # absencia odoo (vacances, baixa...)
+jaume dl # absencia odoo (vacances, baixa...)
+jaume dm # absencia odoo (vacances, baixa...)
+jaume dx # absencia odoo (vacances, baixa...)
+jaume dj # absencia odoo (vacances, baixa...)
+jaume dv # absencia odoo (vacances, baixa...)
+vero dl # absencia odoo (vacances, baixa...)
+tania dm # absencia odoo (vacances, baixa...)
+sergi dl # absencia odoo (vacances, baixa...)
+polmonso dm # absencia odoo (vacances, baixa...)
+polmonso dx # absencia odoo (vacances, baixa...)
+polmonso dj # absencia odoo (vacances, baixa...)
+polmonso dv # absencia odoo (vacances, baixa...)
+erika dx # absencia odoo (vacances, baixa...)
+eloi dv # absencia odoo (vacances, baixa...)
+javi dl # absencia odoo (vacances, baixa...)
+mariabouza dl # absencia odoo (vacances, baixa...)
+mariabouza dm # absencia odoo (vacances, baixa...)
+mariabouza dx # absencia odoo (vacances, baixa...)
+mariabouza dj # absencia odoo (vacances, baixa...)
+mariabouza dv # absencia odoo (vacances, baixa...)
+marc dl # absencia odoo (vacances, baixa...)
+marc dm # absencia odoo (vacances, baixa...)
+marc dx # absencia odoo (vacances, baixa...)
+marc dj # absencia odoo (vacances, baixa...)
+marc dv # absencia odoo (vacances, baixa...)
+maria dv # absencia odoo (vacances, baixa...)
